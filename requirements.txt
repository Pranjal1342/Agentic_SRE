# Core server
fastapi>=0.111.0,<0.113.0
uvicorn[standard]>=0.29.0
websockets>=10.4,<12.0

# Database / ORM
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
pgvector>=0.3.2
alembic>=1.13.1

# Embeddings
sentence-transformers>=3.0.1
numpy>=1.26.0,<2.0.0

# LLM — OpenAI-compatible SDK works for GLM (Zhipu AI), Ollama, and others
openai>=1.35.0
anthropic>=0.28.0

# MCP protocol
mcp>=1.3.0

# Clustering (consolidation job)
scikit-learn>=1.5.0

# Config / utils
pydantic>=2.7.1
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
structlog>=24.2.0

# FAISS for runbook RAG (CPU build)
faiss-cpu>=1.8.0

# Scheduling (consolidation job)
apscheduler>=3.10.4

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.24.1

# UI / Hugging Face Spaces
gradio>=4.36.0
